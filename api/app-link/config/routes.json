{
  "routes": [
    {
      "method": "GET",
      "path": "/app-links",
      "handler": "app-link.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/app-links/count",
      "handler": "app-link.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/app-links/:id",
      "handler": "app-link.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/app-links",
      "handler": "app-link.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/app-links/:id",
      "handler": "app-link.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/app-links/:id",
      "handler": "app-link.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
